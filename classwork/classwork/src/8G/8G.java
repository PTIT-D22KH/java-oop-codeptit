package 8G;

public class 8G {
    public static void main(String[] args) {
        // Write your code here
        Scanner input = new Scanner(System.in);
        String s = input.nextLine();
        String x = input.nextLine();
        for (int i = 0; i < s.length(); i++) {
            if (s.substring(i).contains(x)) {
                
            }
        }
    }
}
